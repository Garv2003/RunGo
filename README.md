# RunGo

A desktop app to run **Go** code locally — a lightweight runner / playground.

> **Work in progress.** Direction: grow into a multi-language *"learn to code"* playground —
> run code in-browser plus guided lessons — on top of a sandboxed execution engine.

## Run locally
```bash
npm install
npm run dev
```
